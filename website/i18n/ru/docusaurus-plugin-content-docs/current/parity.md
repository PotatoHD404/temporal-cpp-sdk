---
title: Возможности и паритет
description: Честная, постатейная матрица того, что работает, в сравнении с официальными Temporal SDK.
---

# Возможности и паритет

**Данный SDK не находится в паритете с официальными Temporal SDK и в ближайшее время им не станет.** Официальные SDK для Go/Java/Python/TS/.NET — это годы работы целых команд; один только движок Temporal на Go занимает десятки тысяч строк, а полная поверхность API (расписания (schedules), Nexus, версионирование, фреймворк для тестирования, безопасность, наблюдаемость, operator/cloud-клиент…) огромна.

Что представляет собой этот проект: подлинно работающее, **полностью протестированное ядро** модели программирования Temporal — достаточное для написания и запуска реальных воркфлоу, которые оркестрируют активности, реагируют на сигналы/запросы (query)/обновления (update), компонуют дочерние воркфлоу и селекторы, а также выполняют continue-as-new — на нативном движке C++ со sticky-кэшем. Эта страница — честная картина текущего состояния.

**Условные обозначения:** ✅ реализовано и протестировано · 🟡 частично · ❌ не реализовано

## Клиент

| Возможность | Статус | Примечания |
|---|---|---|
| Подключение (незащищённое) | ✅ | |
| Запуск воркфлоу | ✅ | id, очередь задач, таймауты, политика повторов |
| Ожидание результата | ✅ | следует по цепочкам continue-as-new |
| Сигнал / Query / Update | ✅ | синхронный query и update |
| Отмена / Завершение | ✅ | |
| Получение дескриптора существующего запуска | ✅ | |
| Signal-with-start | ✅ | `Client::SignalWithStartWorkflow` |
| Список / счётчик / описание воркфлоу | ✅ | `Describe`, `ListWorkflows`, `CountWorkflows` (visibility-запрос) |
| Reset / пакетные операции | ❌ | |
| Клиент расписаний (schedules) | 🟡 | create / describe / delete (интервал + старт воркфлоу); update/list/trigger/pause ❌ |
| Operator и Cloud-сервисы | ❌ | |

## Воркер

| Возможность | Статус | Примечания |
|---|---|---|
| Регистрация воркфлоу / активностей | ✅ | обычные функции `R(Context&, Args...)` |
| Потоки опроса (poller), запуск/остановка/работа | ✅ | |
| Sticky-кэш (резидентные воркфлоу) | ✅ | инкрементальные продолжения по истории |
| Ограниченный LRU-кэш / настройка вытеснения | ✅ | `max_cached_workflows` (вытеснение по LRU) |
| Параллелизм / ограничение частоты (конфигурация) | 🟡 | опции есть; не все применяются |
| Автомасштабирование поллеров, плавный дренаж | ❌ | |
| Версионирование воркеров / Build ID / деплойменты | ❌ | |
| Session workers | ❌ | |

## Написание воркфлоу

| Возможность | Статус | Примечания |
|---|---|---|
| Выполнение активности (типизированное, параллельное) | ✅ | |
| Таймеры (`Sleep` / `NewTimer`) | ✅ | |
| Сигналы (каналы, буферизованные/упорядоченные) | ✅ | |
| Запросы (`SetQueryHandler`) | ✅ | текущее состояние, только чтение |
| Обновления (`SetUpdateHandler`) | ✅ | принятие и завершение на живом пути |
| Валидаторы обновлений | ✅ | read-only валидатор; отклонение эфемерно (не пишется в историю) |
| Селекторы | ✅ | случаи future + приём из сигнальных каналов (`AddReceive`) |
| Дочерние воркфлоу | ✅ | базовые; нет parent-close-policy / отмены / сигнала дочернему |
| Continue-as-new | ✅ | |
| Наблюдение за отменой (`IsCancelled`) | ✅ | |
| Области отмены / распространение | 🟡 | `AwaitCancellation` + отмена таймера и активности `Future::Cancel` ✅; отмена дочерних воркфлоу ❌ |
| `GetVersion` / патчинг | ✅ | на основе маркеров; `kDefaultVersion` на истории до версионирования |
| SideEffect / MutableSideEffect | 🟡 | `SideEffect` ✅ (запись/реплей маркера); MutableSideEffect ❌ |
| Локальные активности | ❌ | |
| Сигнал/отмена внешнего воркфлоу | ❌ | |
| Search attributes / memo / upsert | 🟡 | memo ✅ (старт + `Describe`); search attributes / upsert ❌ |
| Header / распространение контекста | ❌ | |

## Активности

| Возможность | Статус | Примечания |
|---|---|---|
| Типизированное выполнение | ✅ | |
| Повторы на стороне сервера (`RetryPolicy`) | ✅ | |
| Ошибки приложения (с повтором / без) | ✅ | |
| Heartbeat | ✅ | `Context::IsCancelled` видит отмену от сервера; троттлинг ❌ |
| Асинхронное (ручное) завершение | ❌ | |
| Отмена на стороне активности | ✅ | `Future::Cancel` воркфлоу → `RequestCancelActivityTask`; активность видит через `Context::IsCancelled` |

## Данные и сериализация

| Возможность | Статус | Примечания |
|---|---|---|
| JSON / nil / bytes-конвертеры | ✅ | стек по умолчанию на nlohmann-json |
| Пользовательские конвертеры данных | ✅ | |
| Proto / ProtoJSON-конвертеры | ❌ | |
| Payload-кодеки (шифрование/сжатие) | ❌ | |
| Пользовательский конвертер ошибок | ❌ | |
| Большие payload / внешнее хранилище | ❌ | |

## Детерминизм и безопасность

| Возможность | Статус | Примечания |
|---|---|---|
| Диспетчер на stackful-корутинах | ✅ | |
| Sticky-кэш + инкрементальная история | ✅ | |
| Обнаружение недетерминизма | ✅ | воспроизведённые команды сверяются с историей по порядку; `WorkflowPanicPolicy` (блокировать/завершить) |
| Повторное применение обновлений при реплее | ❌ | важно только после вытеснения из кэша |
| Пагинация истории событий | ❌ | длинные истории не разбиваются на страницы |
| Обнаружение дедлоков / политики паник | ❌ | |

## Безопасность, наблюдаемость, экосистема

| Возможность | Статус | Примечания |
|---|---|---|
| TLS / mTLS / аутентификация по API-ключу | ❌ | только незащищённый режим |
| Интерсепторы (клиент + воркер) | ❌ | |
| Метрики | ❌ | |
| Трейсинг / OpenTelemetry | ❌ | |
| Структурированное логирование | ✅ | подключаемый `log::Logger` |
| Фреймворк для тестирования (time-skip, replayer) | 🟡 | replayer ✅ (`Worker::ReplayWorkflowHistory`); time-skip ❌ |
| Расписания (schedules) | 🟡 | create / describe / delete через клиент |
| Nexus-операции | ❌ | |

## Дорожная карта {#roadmap}

Приблизительный порядок приоритетов (подробнее — в `docs/ROADMAP.md` репозитория):

1. **Упрочение детерминизма** — ✅ обнаружение недетерминизма + ограниченный LRU sticky-кэш (`max_cached_workflows`) + обнаружение отмены в heartbeat (`activity::Context::IsCancelled`); осталось: пагинация истории, троттлинг heartbeat.
2. **Поверхность возможностей воркфлоу** — ✅ `SideEffect` + `GetVersion` + валидаторы обновлений + отмена (таймер, активность, `AwaitCancellation`) + случаи channel для селекторов; осталось: отмена дочерних воркфлоу, MutableSideEffect, локальные активности.
3. **Производственные требования** — TLS/mTLS + аутентификация по API-ключу, интерсепторы, метрики и трейсинг, proto/protoJSON-конвертеры + payload-кодеки, настройка воркеров.
4. **Расширение охвата** — ✅ фреймворк для реплея/тестирования + 🟡 расписания (create/describe/delete); осталось: полные расписания (update/list/trigger/pause), Nexus, версионирование воркеров, search attributes, расширенная поверхность клиента.

Если нужная вам возможность находится в столбце ❌ — её действительно ещё нет. Пожалуйста, не делайте противоположных выводов, опираясь на работающее ядро.
